// Your code here
const createEmployeeRecord = arr => {
    const timeInEvents = [];
    for(let i = 0; i < arr.length; i++) {
        if(i === 0) {
            const firstName = arr[i];
        } else if(i === 1) {
            const familyName = arr[i];
        } else if(i === 2) {
            const title = arr[i];
        } else if(i === 3) {
            const payPerHour = arr[i];
        } 
    }
}
